# Astro/Vite public env (build-time). Overrides the default production API base.
# Environment is a DEPLOYMENT concern, not a user-visible UI switch.
# ASTRO_PUBLIC_API_BASE=https://rss-worker-production.weixc0856.workers.dev
